const GameOver = ({ player }) => {
    return (
        <>
            <h4>Game Over </h4>
            <h5>{`Congratulations Player ${player} wins the game`}</h5>
        </>
    )
}

export default GameOver
